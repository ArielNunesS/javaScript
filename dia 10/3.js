const tamanhoArray = parseInt(prompt('Gostaria de inserir quantos números? (1 - 10)'))

let numInserido = 0;
let numArray = 0;
let numeros = [];

for(tamanhoArray; numArray < tamanhoArray; numArray++){
        numInserido = parseFloat(prompt('Insira um número:'))
    
        numeros[numArray] = numInserido
}
        console.log(numeros)
        console.log(numeros.reverse())